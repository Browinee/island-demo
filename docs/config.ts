import { defineConfig } from "../dist";

export default defineConfig({
  title: "xxx",
  themeConfig: {
    nav: [
      { text: "Main", link: "/" },
      { text: "Guide", link: "/guide/" },
    ],
  },
});
